# LF в репозитории и в рабочей копии. Автоконвертация Windows превращает diff
# в перечисление всех строк файла и уже однажды в этом проекте испортила текст
# при массовой правке — здесь ничего не должно зависеть от настроек машины.
* text=auto eol=lf

*.png binary
*.ico binary
*.exe binary
